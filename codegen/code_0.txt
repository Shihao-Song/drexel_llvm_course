def<int> add(int x, int y)
{
    set<int> z = x + y;
    return z;
}

main()
{
    set<int> x = -80;
    set<int> y = 40 - x;
    set<int> z = x + y + -40;
    printVarInt(z);
    set<int> z = z - y - -80;
    printVarInt(z);
  
    return 0;
}
