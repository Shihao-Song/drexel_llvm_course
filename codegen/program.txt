def<int> add(int x, int y)
{
    set<int> z = x + y;
    return z;
}

main()
{
    set<int> x = 10;
    set<int> y = -20;
    set<int> z = x + y;
    printVarInt(z);
    set<int> z = add(x, -20);
    printVarInt(z);
    return 0;
}
