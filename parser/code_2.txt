def<int> add(int a, int b)
{
    set<int> c = a + b;
    return c;
}

main()
{
    set<int> x = 5;
    set<int> y = 10;
    printVarInt(y);
    set<float> k = 12.5;
    printVarFloat(k);
    set<int> result = add(x, y);
    set<int> z = 7 + 3 * (10 / (12 / add(3, 1) - 1));
    set<int> z = add(5, 3) * 12  + add(3, 4) * (10 + 5);
    set<int> z = 1 + 2 * add(3, 4) + 5 * 8;
    set<int> z = 1 + 2 + add(3, 4) + (5 + 6);
    set<int> z = add(3,4) * add(4,5) + 2
}
