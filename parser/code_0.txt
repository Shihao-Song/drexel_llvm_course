main()
{
    set<int> x = 10;
    set<int> y = 20;
    set<int> z = x + y - 24 + 1 + 100;
    set<int> z = x + y * 25 * 74 / 124 + 25 + 68;
    set<float> z = 2.5 + 12.5;
    set<int> z = y * 25 * 74 / 124 + x;
    set<int> x = (1 + (1 + 4) * 5 + (7 - 8) / 10) * 100;
    set<int> y = x * y * z * (1 + 2);
    set<int> z = 7 + 3 * (10 / (12 / (3 + 1) - 1));
    set<int> z = (5 + 3) * 12  + (3 + 4) * 10 + 5;
    set<int> z = 1 + 2 * (3 + 4) + 5;
    set<int> z = 1 + 2 + 3 + 4 + 5;
}
