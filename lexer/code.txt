def<int> add(int x, int y)
{
    set<int> z = x + y;
    return z;
}

main()
{
    set<int> x = 5;
    set<int> y = 10;
    set<float> z = 4.5 + 7.9;
    set<int> result = add(x, y);
    printVarInt(z);
}
